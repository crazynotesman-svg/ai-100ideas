# 2026-08-10

## Phase 1 — ai.100ideas.net 项目初始化

在工作区根目录搭建 Astro 7 + Tailwind 4 + Cloudflare D1 + Drizzle 的开源 AI 工具/MCP 目录站基础。

已完成：
- `npm create astro` minimal 模板（TS strict），加 `@tailwindcss/vite`、`@astrojs/sitemap`、`@astrojs/cloudflare` v14。
- `wrangler.toml`（name = ai-100ideas，D1 binding `DB` + 占位 database_id，`nodejs_compat`），删掉适配器默认生成的 `wrangler.jsonc`。
- `src/db/schema.ts`：categories / tools / submissions；生成 `drizzle/migrations/0000_init.sql` 与根目录 `schema.sql` 快照。
- `src/layouts/BaseLayout.astro`：title/description/keywords、canonical、OG、Twitter Card、JSON-LD 注入。
- 首页 `src/pages/index.astro`、`src/pages/robots.txt.ts`、`public/og-default.png`（脚本生成的占位图）。
- `astro check` 0 error；`npm run build` 通过；git 初始化并提交 2 个 commit。

## 踩坑记录（重要）

1. **本机 workerd 崩溃**：`wrangler types` 和 `astro build` 的预渲染都会触发
   `structured exception #0xc0000005: access violation`（沙箱内外均复现，属本机 MSVC Redistributable 问题）。
   - 解法：`cloudflare({ prerenderEnvironment: 'node' })` 绕过 workerd 预渲染；
     `worker-configuration.d.ts` 手写（`/// <reference types="@cloudflare/workers-types" />` + `Cloudflare.Env`）。
   - 修好 VC++ 运行库后可移除这两处 workaround。
2. **@astrojs/cloudflare v13+ 已移除 `Astro.locals.runtime`**，绑定改为 `import { env } from 'cloudflare:workers'`。
3. `build.format: 'file'` 会让 `Astro.url.pathname` 变成 `/index.html`，canonical/og:url 必须做路径归一化，
   才能和 sitemap 的 `<loc>` 完全一致。

## Phase 2 — 抓取 + DeepSeek 富化 + D1 同步流水线

新增 `scripts/`：`types.ts`、`util.ts`、`fetcher.ts`（GitHub Search API 多 topic + README 抓取 + topic 标注）、
`enrich.ts`（deepseek-chat + json_object + 容错解析 + 确定性 fallback）、`sync.ts`（INSERT…ON CONFLICT upsert，
默认走 D1 REST API，可切 wrangler CLI）、`run-pipeline.ts`（编排，支持 `--dry-run/--max-repos/--no-enrich/--topic`）。
`scripts/fixtures/sample-repos.json` 离线样例；`.github/workflows/daily-sync.yml`（每日 02:00 UTC + workflow_dispatch）。
`package.json` 加 `sync`、`sync:dry-run`、`enrich:test`，并加 `tsx` devDep。

验证：`GITHUB_FIXTURE=... npm run sync:dry-run` 离线端到端通过；生成的 `dry-run.sql` 用 node:sqlite 实跑 3 行工具+3 分类正确；
`astro check` 0 error（18 文件）。

注意点：
- tsconfig 含 `**/*` 且 `verbatimModuleSyntax: true`，脚本里类型导入必须用 `import type`。
- GitHub Search 结果不含 `topics`，`fetcher.ts` 改为用搜索用的 topic 反标到 repo.topics（用于 is_mcp/分类判断）。
- D1 写入默认 `SYNC_METHOD=rest`（D1 REST `/query` statements 批处理），需要 CLOUDFLARE_ACCOUNT_ID/DATABASE_ID/TOKEN。
