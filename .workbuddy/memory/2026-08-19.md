# 2026-08-19

## 程序化 SEO 标签页（Tag Aggregation Pages）

按队长需求实现 `/tag/[tag]` 长尾 SEO 页 + 工具详情页 Related Tools 内链网。

- **发现根因**：原 D1 schema 无 `tags` 列，工具只有 `techStack`/`categoryId`/`isMcp`/`alternativeTo`。队长选定「新增 `tags` 列 + 免费本地回填（不烧 DeepSeek token）」方案。
- 改动：`src/lib/tags.ts`（deriveTags/effectiveTags/tagLabel + local-llm 语义映射）、`src/db/queries.ts`（getAllTags/getToolsByTag/getTagBySlug/getRelatedTools）、`src/pages/tag/[tag].astro` + `src/pages/tags/index.astro`、`ToolCard`/`tool/[slug].astro`/`Header`/`Footer` 内链、`sitemap.xml.ts` 增加 tag URL、`scripts/backfill-tags.ts` + `.github/workflows/backfill-tags.yml`、`schema.ts` 加 `tags` 列、`drizzle` 迁移 `0001_vengeful_sphinx.sql`（`ALTER TABLE tools ADD tags text`）。
- `astro check` 0 错误；`deriveTags` 逻辑离线验证通过（Ollama→local-llm、C++→cpp 正确）。
- **PAT 现状**：`ghp_Kqp3...` 现已 401（之前那次「手动触发 sync」其实 dispatch 失败、从未真正跑过，成本闸门仅离线验证）。**代码已本地提交，但部署需队长提供新的 repo+workflow PAT** 才能 push + 触发回填。
