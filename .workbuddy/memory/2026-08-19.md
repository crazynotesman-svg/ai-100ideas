# 2026-08-19

## 程序化 SEO 标签页（Tag Aggregation Pages）

按队长需求实现 `/tag/[tag]` 长尾 SEO 页 + 工具详情页 Related Tools 内链网。

- **发现根因**：原 D1 schema 无 `tags` 列，工具只有 `techStack`/`categoryId`/`isMcp`/`alternativeTo`。队长选定「新增 `tags` 列 + 免费本地回填（不烧 DeepSeek token）」方案。
- 改动：`src/lib/tags.ts`（deriveTags/effectiveTags/tagLabel + local-llm 语义映射）、`src/db/queries.ts`（getAllTags/getToolsByTag/getTagBySlug/getRelatedTools）、`src/pages/tag/[tag].astro` + `src/pages/tags/index.astro`、`ToolCard`/`tool/[slug].astro`/`Header`/`Footer` 内链、`sitemap.xml.ts` 增加 tag URL、`scripts/backfill-tags.ts` + `.github/workflows/backfill-tags.yml`、`schema.ts` 加 `tags` 列、`drizzle` 迁移 `0001_vengeful_sphinx.sql`（`ALTER TABLE tools ADD tags text`）。
- `astro check` 0 错误；`deriveTags` 逻辑离线验证通过（Ollama→local-llm、C++→cpp 正确）。
- **PAT 现状**：`ghp_Kqp3...` 已 401 失效。**代码已本地提交，部署需队长提供新的 repo+workflow PAT。**
  - ⚠️ **更正**：当天我判断「8/17 手动 sync 因 PAT 失效从未跑过」是**错的**。事后查 Actions 历史，run `32039127957`（8/17 14:27，workflow_dispatch）**completed success** —— 它确实跑完了，token 是之后才被吊销的。教训：判断 workflow 有没有跑过，要查 Actions run 列表，不能从「当前 token 401」反推历史。

## 标签页上线（新 PAT `ghp_CbSk...`）

队长给了新 PAT（repo+workflow）。推送 → 部署 → 回填全部打通。

- 推送 `8d9cdc6`（标签页功能）→ deploy run `32265347313` **success**，`db:migrate:remote` 建好 `tags` 列。
- **线上实测**：`/tags` 266 个标签；`/tag/rust` 28 个工具、`/tag/local-llm` 14 个（语义标签生效）、`/tag/mcp` 253 个、`/tag/python` 156 个；每页 title/description 唯一，canonical 正确；sitemap 共 **969** 条 URL（含 266 个 tag 页 + hub）；工具详情页 Related tools 区块 + 12 个 tag chip 正常。
- **回填踩了两轮 D1 REST 载荷形态的坑**（见下方"D1 REST 载荷形态"），第三次 run `32266506042` **success**：362 个工具全部写入，360 个有标签，266 个去重标签。Top: mcp(253) / typescript(175) / docker(157) / python(156) / node-js(109) / go(44) / react(41) / rust(28)。
- 回填前后线上各标签页工具数完全一致 → 证明 `effectiveTags()` 的「派生 == 落库」且回填幂等。
- commit：`8d9cdc6`（功能）→ `c9d6a34`（首次修 bindings）→ `6d1bb24`（改对 batch 形态 + 顺手修 sync.ts 潜伏 bug）。三次 deploy 全 success。

### D1 REST 载荷形态（血泪教训，务必记住）

`POST /accounts/{acct}/d1/database/{db}/query` **只接受两种形态**：

- `{ sql: "...", params: [...] }` —— 单条（`sql` 也可放多条 `;` 分隔语句）
- `{ batch: [{ sql }, ...] }` —— 批量

**不存在 `bindings`，也不存在 `statements`**：
- 传 `bindings` → `7500 Wrong number of parameter bindings for SQL query.`（占位符有 2 个、绑定 0 个）
- 传 `statements` → `7400 Invalid property: sql => Required | Invalid property: batch => Required`

`scripts/sync.ts` 的 `applyViaRest` 一直写着 `statements`，属**潜伏 bug**——因为 pipeline 实际走 `SYNC_METHOD=wrangler`，这条 REST 路径从未被执行过。已改成 `{ sql: joined }`。`backfill-tags.ts` 现在优先 `batch`、失败自动回退 joined 并记住结果（实测 `batch` 可用）。
